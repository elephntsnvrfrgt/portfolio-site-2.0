mixin mixin-hero(eyebrow, title, info)
  .content-hero
    .hero-eyebrow= eyebrow
    h2.hero-title= title
    .hero-info= info
  
mixin mixin-experience(job, years)
  li.experience
    p= job
    .small= years
